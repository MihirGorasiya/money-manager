name: money_manager
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  animated_bottom_navigation_bar: ^1.3.0
  collection: ^1.18.0
  flutter:
    sdk: flutter
  get: ^4.6.6
  intl: ^0.19.0
  path_provider: ^2.1.1
  sqflite: ^2.3.0

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
